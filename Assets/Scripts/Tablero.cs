using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Tablero : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        var pos0 = GameObject.Find("TableroPos0").transform.position;
        var pos1 = GameObject.Find("TableroPos1").transform.position;
        var pos2 = GameObject.Find("TableroPos2").transform.position;


    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
